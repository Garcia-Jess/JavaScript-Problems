// Problema:
// Escreva uma função chamada repeatCharacter que recebe dois parâmetros: um caractere e um número.
// A função deve retornar o caractere repetido o número de vezes especificado.

function repeatCharacter(str, num) {
  return str.repeat(num);
}

console.log(repeatCharacter("a", 5));
