//Write a JavaScript function that takes two numbers as input and returns their sum.

//Requirements:
//You need to create a function called sum.
//The function should accept two parameters: num1 and num2.
//Return the sum of num1 and num2.

function sum(num1, num2) {
  return num1 + num2;
}

console.log(sum(4, 5));
