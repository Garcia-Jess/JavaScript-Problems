//Problem 02: Convert Minutes to Seconds
//Write a JavaScript function that converts a given number of minutes into seconds.

//Requirements:
//Create a function called convertToSeconds.
//The function should accept one parameter: minutes.
//Return the number of seconds in the given minutes (i.e., minutes * 60).

function convertToSeconds(minutes) {
  return minutes * 60;
}

console.log(convertToSeconds(5));
